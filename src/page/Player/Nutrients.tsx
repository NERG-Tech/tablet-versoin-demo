import React from 'react';
import {Text, View} from 'react-native';

const NutrientsScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
      }}>
      <Text style={{textAlign: 'center'}}>Nutrients</Text>
    </View>
  );
};

export default NutrientsScreen;
