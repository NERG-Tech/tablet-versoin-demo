import React from 'react';
import {Text, View} from 'react-native';

const GeneticsScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
      }}>
      <Text style={{textAlign: 'center'}}>Genetics</Text>
    </View>
  );
};

export default GeneticsScreen;
